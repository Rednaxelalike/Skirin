/// <reference types="vite/client" />

// TypeScript 7 wants a declaration behind every side-effect import, including
// `import './styles.css'`. Vite's client types supply those, along with the
// `import.meta.env` shape.
